下面代码截取自 Linux 0.12, 请问 `task[1]->p_cptr->p_ysptr = p;` 看上去是把最老的子进程赋值给了 init 原来子进程最年轻的子进程, 这块不应该直接赋值 `current->p_cptr` 吗?

```c
p->p_osptr = task[1]->p_cptr;
task[1]->p_cptr->p_ysptr = p;
task[1]->p_cptr = current->p_cptr;
current->p_cptr = 0;
```