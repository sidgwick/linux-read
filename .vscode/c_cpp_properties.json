{
  "configurations": [
    {
      "name": "Linux",
      "includePath": [
        "${workspaceFolder}/include/**", // 项目内自定义头文件路径（优先级最高）
        "${workspaceFolder}/**", // 工作区所有子目录
        "/usr/include/**" // 系统路径（置后）
      ],
      "compilerPath": "/usr/bin/gcc", // 指定编译器路径
      "intelliSenseMode": "linux-gcc-x86"
    }
  ],
  "version": 4
}